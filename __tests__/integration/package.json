{
  "name": "@ainative/ai-kit-integration-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Integration tests for AINative AI Kit",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "bench": "vitest bench",
    "bench:watch": "vitest bench --watch"
  },
  "devDependencies": {
    "@testing-library/react": "^14.0.0",
    "@types/node": "^20.10.0",
    "@vitest/coverage-v8": "^1.0.0",
    "@vitest/ui": "^1.0.0",
    "happy-dom": "^12.10.3",
    "jsdom": "^23.0.0",
    "msw": "^2.0.0",
    "vitest": "^1.0.0"
  }
}
